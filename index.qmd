---
title: "Probability for Data Science"
format:
  html: default
  revealjs:
    output-file: index-slides.html
  pdf:
    output-file: index-handout.pdf
  docx:
    output-file: index.docx
---

# Welcome {.unnumbered}

These notes collect the probability that data science courses assume.

## Using these notes in another site

Course sites include these notes as a git submodule named `pds`
at the site's root,
and include fragments with paths that start with `pds/`.

Quarto resolves `@id` cross-references only within one rendered page,
so a host site that links to a result here
uses an explicit link to the page and anchor.
